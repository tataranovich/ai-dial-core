name: Dependency Submission

on:
  push:
    branches: [ 'test-ci' ]

jobs:
  dependency-submission:
    uses: epam/ai-dial-ci/.github/workflows/gradle-dependency-submission.yml@gradle-dependency-submission
