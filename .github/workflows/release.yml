name: Release Workflow

on:
  push:
    branches: [development, release-*]

jobs:
  release:
    uses: tataranovich/ai-dial-ci/.github/workflows/java_release.yml@gh-e2e-test
    secrets: inherit
    with:
      platforms: "linux/amd64,linux/arm64"
      java-version: 21
